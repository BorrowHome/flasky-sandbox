#docker-compose -f xxxxx.yml up -d

version: '2'
services:
  web:
    image: liliangbin/sandbox:v1
    privileged: true
    command: ./run.sh
    ports:
      - "8080:8080"
    volumes:
      - /root/video:/flask-sandbox/app/static/video